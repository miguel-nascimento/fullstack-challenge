query GetBooks($cursor: String, $limit: Float, $title: String) {
  getBooks(cursor: $cursor, limit: $limit, title: $title) {
    id
    title
    subtitle
    description
    author
    image
    createdAt
  }
}
